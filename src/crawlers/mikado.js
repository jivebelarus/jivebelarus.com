const logger = require('../logger')('mikado');
const config = require('config');
const fetch = require('node-fetch');
const sum = require('lodash/sum');


const mikado = async () => {
    return 34659; // ended
}

module.exports = mikado;